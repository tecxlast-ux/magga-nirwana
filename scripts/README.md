# Scripts

Utility scripts for managing configurations and monitoring data.

- `sanitize-routeros-export.sh` – strip secrets from RouterOS exports.
- `backup-configs.sh` – backup device configs to storage.
- `generate-targets-from-topology.py` – create Prometheus target files from topology.
- `validate-topology.py` – basic schema validation for `topology.yaml`.
