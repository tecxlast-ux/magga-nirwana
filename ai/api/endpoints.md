# API Endpoints

- `GET /reports` – List available reports with pagination.
- `GET /reports/{report_id}` – Retrieve a specific report in normalized format.

All endpoints are read-only and should enforce authentication via API keys or OAuth.
