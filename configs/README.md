# Configurations (Sanitized)

Store only sanitized configuration exports. Remove secrets, keys, and identifiable hostnames before committing.

- `routeros/` – RouterOS exports for hEX and L009 devices.
- `ap/` – Access point baseline configuration notes.
