groups:
  - name: latency
    rules:
      - alert: HighLatency
        expr: probe_duration_seconds > 0.5
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High latency detected"
          description: "Blackbox probe latency above 500ms for more than 5 minutes."
